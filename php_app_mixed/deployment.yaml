apiVersion: apps/v1
kind: Deployment
metadata:
  name: phpapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phpapp
      version: v1
  template:
    metadata:
      labels:
        app: phpapp
        version: v1
    spec:
      volumes:
      - name: nginx-config-volume
        configMap:
          name: nginx-config
      - name: shared-files
        emptyDir: {}
      containers:
      - image: psibi/cake-php-demo:v1.0.2
        securityContext:
          runAsUser: 0
        imagePullPolicy: IfNotPresent
        name: phpapp
        volumeMounts:
        - name: shared-files
          mountPath: /var/www-data
          readOnly: false
        lifecycle:
          postStart:
            exec:
              command: ["/bin/sh", "-c", "cp -r /app/* /var/www-data && chmod -R 666 /var/www-data/logs && chmod -R 666 /var/www-data/tmp && chown -R www-data:www-data /var/www-data"]
      - image: nginx:1.23.1
        name: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config-volume
          mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
        - name: shared-files
          mountPath: /var/www-data
          readOnly: false
